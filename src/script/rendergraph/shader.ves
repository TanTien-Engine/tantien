import "render" for Shader as r_Shader
import "blueprint.node" for Node
import "blueprint.pin" for Pin

class Shader is Node
{
	init() 
	{
		super.init(Shader.name)

		this.exports = [
			Pin(this, "out", "shader")
		]
		this.layout()

		this.vs = nil
		this.fs = nil

		this.prog = nil
	}

	execute() {
		if (this.vs and this.fs) {
			this.prog = r_Shader.init(this.vs, this.fs)		
		}
	}

	toString(name) {
		return "
%(name).vs = \"%(this.vs)\"
%(name).fs = \"%(this.fs)\"
"
	}
}