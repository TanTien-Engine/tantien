import "math" for Math
import "maths.vector" for Vector2
import "maths" for Float2
import "blueprint.blueprint" for Blueprint
import "blueprint.node_render" for NodeRender
import "blueprint.dirty_types" for DIRTY_CONNECTION
import "blueprint.node_layout" for DEFAULT_HEIGHT
import "blueprint.node_param" for NodeParam
import "blueprint.pin_color_map" for PIN_COLOR_MAP
import "blueprint.node_style" for COL_PANEL_BG
import "blueprint.dirty_types" for DIRTY_GUI

class Style
{
	init()
	{
		this.width = 0
		this.height = 0

		this.line_num = 0

		this.color = COL_PANEL_BG
	}
}

class Node
{
	init() 
	{
		this.imports = []
		this.exports = []

		this.style = Style()

		this.pos = Vector2()

		this.params = []

		// for aabb update
		this.aabb_dirty = false

		this.dummy = false

		// false = the editor skips drawing this node (and its incoming wire).
		// Used for pure-plumbing nodes (e.g. the rebuild's Cache wrappers) that
		// must exist for evaluation but should not clutter the graph.
		this.visible = true

		this.selected = false

		// Optional per-instance display name. nil = fall back to the class
		// name (see get_title). Must be initialised here: Vessel raises on
		// reading an unset field, and get_title reads it for every node.
		this.title = nil

		this.dirty_timestamp = 0
		this.msg_timestamp = 0

		// debug
		this.calc_times = 0
		this.render_times = 0

		// --- render cache (consumed by NodeRender) ---
		// The canvas replays these built Painters/text every frame and only
		// rebuilds them when render_dirty is set, killing the per-frame
		// Painter.init() GC churn. render_dirty starts true so the first draw
		// builds; render_lod tracks the scale<2 LOD band the cache was built for.
		this.render_dirty = true
		this.render_lod = nil
		this.cache_painters = nil
		this.cache_texts = nil
	}

	get_size() {
		return Vector2(this.style.width, this.style.height)
	}

	update(mt)
	{
		var pos = Float2.init()
		pos.transform(mt)
		if (this.pos.x == pos.x() and this.pos.y == pos.y()) {
			return
		}
		this.pos.x = pos.x()
		this.pos.y = pos.y()

		// Moved -> this node's panel/pins/own-wires are stale (early-out above
		// means this only fires on a real move, not every frame). Incident wires
		// owned by OTHER nodes are refreshed via the c.update() calls below, which
		// dirty their owner in Connecting.update.
		this.render_dirty = true

		for (var p in this.imports) {
			for (var c in p.conns) {
				c.update()
			}
		}
		for (var p in this.exports) {
			for (var c in p.conns) {
				c.update()
				// A hidden pass-through (a parked Cache) owns the DRAWN wire from
				// itself onward, but that wire is only recomputed when the cache or
				// its consumer moves -- never when THIS producer moves. So refresh
				// the hidden node's outgoing wires too; their start resolves back to
				// this producer's live pin (Connecting.resolve_from), so the drawn
				// link tracks the producer as it is dragged. No-op for visible
				// targets (ordinary graphs).
				if (!c.to.node.visible) {
					c.to.node.update_hidden_out()
				}
			}
		}
	}

	// Recompute the outgoing (drawn) wires of a hidden pass-through node and any
	// hidden nodes chained after it. Called from a visible producer's update so a
	// parked Cache's wire follows the producer (see update). The chain ends once a
	// visible consumer is reached.
	update_hidden_out()
	{
		for (var p in this.exports) {
			for (var c in p.conns) {
				c.update()
				if (!c.to.node.visible) {
					c.to.node.update_hidden_out()
				}
			}
		}
	}

	draw(mt, cam_mt)
	{
		NodeRender.draw_node(this)
	}

	layout()
	{
		this.style.width = 120
		this.style.height = this.calc_panel_height()

		for (var i = 0; i < this.imports.count; i = i + 1) {
			this.imports[i].is_input = true
			this.imports[i].slot_idx = i
		}
		for (var i = 0; i < this.exports.count; i = i + 1) {
			this.exports[i].is_input = false
			this.exports[i].slot_idx = i
		}

		this.aabb_dirty = true
		// Size / pin-set / pin-color all changed here (construction, dynamic-pin
		// rebuilds, Commentary resize) -> the cached panel/pins go stale.
		this.render_dirty = true

		for (var p in this.params) {
			this.style.height = this.style.height + DEFAULT_HEIGHT * p.gui_height()
		}

		// pin color
		for (var p in this.imports) {
			this.init_pin_color(p)
		}
		for (var p in this.exports) {
			this.init_pin_color(p)
		}

		// A re-layout (dynamic pin add/remove, or a height change) moves this
		// node's pin positions WITHOUT moving the node, so every wire touching it
		// now has a stale endpoint -- and a wire's curve is cached on the source
		// node, not recomputed at draw. Refresh every incident wire (mirrors what
		// update() does on a move): c.update() rebuilds the Bezier against the new
		// positions AND dirties each wire's owning source node via Connecting.update.
		// No-op at construction (pins have no conns yet).
		for (var p in this.imports) {
			for (var c in p.conns) {
				c.update()
			}
		}
		for (var p in this.exports) {
			for (var c in p.conns) {
				c.update()
				if (!c.to.node.visible) {
					c.to.node.update_hidden_out()
				}
			}
		}
	}

	init_pin_color(pin)
	{
		var type = pin.type
		if (pin.type is List) {
			type = pin.type[0]
		}	

		var col = PIN_COLOR_MAP[type]
		if (col) {
			pin.color = col
		}
	}

	to_string(name)
	{
		var ret = ""
		for (var p in this.params) {
			ret = ret + p.to_string(name)
		}
		if (this.title and this.title != "") {
			ret = ret + "%(name).title = \"%(this.title)\"\n"
		}
		return ret
	}

	draw_gui(ctx) 
	{
		var dirty = false

		var pos = Vector2()
		pos.x = this.pos.x - this.style.width * 0.5
		pos.y = this.pos.y + this.style.height * 0.5 - this.calc_panel_height() - DEFAULT_HEIGHT

		for (var p in this.params) {
			if (p.draw_gui(ctx, pos, this.style.width)) {
				dirty = true
			}
		}		

		return dirty
	}

	on_selected()
	{
		this.selected = true
		this.on_render_select_changed()
	}

	on_deselected()
	{
		this.selected = false
		this.on_render_select_changed()
	}

	// Selecting a node flips its own pin visibility (is_pin_visible) AND the
	// visibility of every wire touching it (is_conn_visible fires when EITHER
	// endpoint is selected). A wire is drawn by its SOURCE node, so the outgoing
	// wires this node owns are covered by dirtying self; its INCOMING wires are
	// owned by their producers, so dirty those producers too.
	on_render_select_changed()
	{
		this.render_dirty = true
		for (var p in this.imports) {
			for (var c in p.conns) {
				c.from.node.render_dirty = true
			}
		}
	}

	on_connected(pin) {}
	on_disconnected(pin) {}

	on_key_pressed(key) {}
	on_key_released(key) {}
	on_mouse_pressed(x, y, btn) {}
	on_mouse_released(x, y, btn) {}
	on_mouse_moved(x, y, btn) {}
	on_mouse_dragged(x, y, btn) {}
	on_mouse_clicked(x, y, btn) {}
	on_mouse_scrolled(x, y, dx, dy) {}

	on_pin_dirty(pin, type, msg)
	{
		if (type == DIRTY_CONNECTION) {
			// A wire added/removed here changes this node's drawn pin fill
			// (outline vs filled, pin.conns.isEmpty) and wire set. This is
			// broadcast to every affected pin's node, so both endpoints rebuild.
			this.render_dirty = true
		}

		if (type == DIRTY_CONNECTION and pin.is_input) {
			this.on_connection_dirty()
		}

		var t = pin.dirty_timestamp
		if (pin.is_input)
		{
			// one-way
			for (var p in this.imports) {
				p.dirty_timestamp = Math.max(p.dirty_timestamp, t)
			}
			for (var pin in this.exports) {
				Blueprint.send_pin_dirty(pin, type, msg)
			}
		}
		else
		{
			// one-way
			for (var p in this.exports) {
				p.dirty_timestamp = Math.max(p.dirty_timestamp, t)
			}
			for (var pin in this.imports) {
				Blueprint.send_pin_dirty(pin, type, msg)
			}
		}
	}
	on_connection_dirty() {}
	on_node_dirty() {}

	on_param_dirty()
	{
		// Conservative: a param edit is event-scoped (fires only when a widget
		// actually changes a value, not per frame). Param widgets are drawn by the
		// GUI layer, not the cached Painter, so this rarely changes the cache; mark
		// dirty anyway so any param that does affect the panel stays correct.
		this.render_dirty = true

		// todo: only use pin_dirty ?
		Blueprint.send_node_dirty_root(this)
		for (var pin in this.exports) {
			Blueprint.send_pin_dirty_root(pin, DIRTY_GUI)
		}
	}

	on_scene_add(editor) {}

	calc_panel_height() {
		return DEFAULT_HEIGHT + Math.max(this.imports.count, this.exports.count) * DEFAULT_HEIGHT
	}

	disconnect()
	{
		if (this.imports.count == 1 and 
		    this.exports.count == 1 and 
		    !this.imports[0].conns.isEmpty and 
		    !this.exports[0].conns.isEmpty)
		{
			var from = this.imports[0].conns[0].from
			var to = this.exports[0].conns[0].to
			Blueprint.connect(from, to)
		}

		for (var p in this.imports) {
			while (!p.conns.isEmpty) {
				Blueprint.disconnect(p.conns.front())
			}
		}
		for (var p in this.exports) {
			while (!p.conns.isEmpty) {
				Blueprint.disconnect(p.conns.front())
			}
		}
	}

	query_param(name)
	{
		for (var p in this.params) {
			if (p.name == name) {
				return p
			}
		}	
		return nil	
	}

	get_title()
	{
		if (this.title and this.title != "") {
			return this.title
		}
		return this.get_class().name
	}

	bind_editor() { return false }
	draw_editor() {}
}