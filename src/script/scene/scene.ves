class Scene
{
	init() {
		this.nodes = []
		this.selection = Set.new()
	}

	add(node) {
		this.nodes.add(node)
	}

	update()
	{
		for (var node in this.nodes) {
			node.update()
		}	
	}

	draw() 
	{
		for (var node in this.nodes) {
			node.draw()
		}
	}
}