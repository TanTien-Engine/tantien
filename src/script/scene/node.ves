class Node
{
	init() {
		this.components = {}
	}

	draw() 
	{
		var mt = nil
		var trans = this.components["transform"]
		if (trans) {
			mt = trans.get_matrix()
		}

		for (var entry in this.components) {
			entry.value.draw(mt)
		}
	}
}