import "blueprint.node" for Node
import "blueprint.pin" for Pin
import "blueprint.variant" for VAR_TYPE_NUMBER3

class Normal is Node
{
	init()
	{
		super.init(Normal.name)	

		this.exports = [
			Pin(this, "normal", VAR_TYPE_NUMBER3),
		]

		this.layout()
	}

	shader_gen(ctx)
	{
		super.shader_gen(ctx)

		this.inst = ctx.gen.add_input("Normal", "vec3")
	}

	calc_inst(idx, ctx) 
	{
		return ctx.bb.load(this.inst)
	}
}