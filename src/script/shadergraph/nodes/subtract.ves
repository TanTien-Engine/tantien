import "blueprint.nodes.subtract" for Subtract as bp_Subtract
import "blueprint.blueprint" for Blueprint

class Subtract is bp_Subtract
{
	init()
	{
		super.init()	
	}

	calc_inst(gen, idx, module, func) 
	{
		var a = Blueprint.calc_input_inst(gen, this, 0, module, func)
		var b = Blueprint.calc_input_inst(gen, this, 1, module, func)
		
		if (!a and !b) {
			return nil
		} else if (!a) {
			return b
		} else if (!b) {
			return a
		} else {
			return gen.sub(func, a, b)
		}
	}
}