import "physicsgraph.nodes.*"

var PHYSICS_NODES = [ 
// objects
World, Body, BodyInfo,
// joints
PrismaticJoint,
// operate
Simulation, ClearWorld, RemoveBody, SetLinearVelocity, SetTransform, UpdateWorld,
// events
BeginContact, EndContact, PreSolve, PostSolve,
// tools
BodySelector,
]