import "blueprint.nodes.*"

var BP_NODES = [ 
// math
Boolean, Integer, Number, Number2, Number3, Number4, Matrix, Add, Subtract, Multiply, Divide, Radians, Sin, Cos, Clamp, Reciprocal, Negate,
// camera
Camera2d, Camera3d, OrbitCamera, Lookat, Orthographic, Perspective,
// transform
Scale, Rotate, Translate,
// constant
Time, Viewport,
// data
Array, Combine, Fetch, Merge, Split, ClearList, InsertToList,
// control flow
Loop, ForEach, Branch, Compare, Finish, Group,
// tools
Commentary, Custom, Print, Proxy, Expand, Store, Load, Variable, Swap,
// subgraph
Input, Output, Property, Subgraph,
]
