import "render" for Render
import "blueprint.node" for Node
import "blueprint.port" for Port

class Draw is Node
{
	init()
	{
		super.init(Draw.name)

		this.imports = [
			Port(this, "prev", "port")
		]
		this.exports = [
			Port(this, "next", "port")
		]

		this.prim_type = "triangles"

		this.prog = 0
		this.va = 0

		this.render_state = {}
	}

	execute() {
		Render.draw(this.prim_type, this.prog, this.va, this.render_state)
	}
}